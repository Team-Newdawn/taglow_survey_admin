# `src/view/admin/analysis/css` Instructions

Owns page-level CSS for `SurveyAnalysisPage`.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Page CSS owns workbench grid, sticky filter bar placement, and card region spacing.
- Do not style individual analysis cards through page selectors.
