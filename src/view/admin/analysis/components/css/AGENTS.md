# `src/view/admin/analysis/components/css` Instructions

Owns component CSS for analysis cards and controls.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Component CSS owns internal chart, table, badge, heatmap, and empty-state styling.
- Do not depend on `SurveyAnalysisPage.css` selectors.
