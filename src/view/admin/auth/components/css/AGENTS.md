# `src/view/admin/auth/components/css` Instructions

Owns component CSS for auth feature components.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Keep component styles independent from auth page classes.
