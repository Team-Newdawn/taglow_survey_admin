# `src/view/admin/settings/css` Instructions

Owns page-level CSS for survey settings pages.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Page CSS owns layout, section spacing, and route-level responsive behavior.
- Component-level settings widgets should use local component CSS if a `components` folder is added later.
