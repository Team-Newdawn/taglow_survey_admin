# `src/view/admin/preview/components/css` Instructions

Owns component CSS for preview controls.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Component CSS owns toolbar controls, device frame internals, and scenario selector states.
- Do not call or imply response persistence in styling or class names.
