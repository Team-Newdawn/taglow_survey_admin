# `src/view/admin/auth/css` Instructions

Owns page-level CSS for admin auth pages.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Page CSS owns login/access layout only.
- Auth component internals belong in `components/css`.
