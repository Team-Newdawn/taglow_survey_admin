# `src/view/admin/surveys/css` Instructions

Owns page-level CSS for survey list and dashboard pages.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Page CSS owns list/dashboard composition and route-level responsive behavior.
- Survey card/table internals belong in `components/css`.
