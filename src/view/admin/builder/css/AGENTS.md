# `src/view/admin/builder/css` Instructions

Owns page-level CSS for `SurveyBuilderPage`.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Page CSS owns builder shell layout, panel columns, and route-level responsive behavior.
- Builder component internals belong in `components/css`.
