# `src/view/admin/system/css` Instructions

Owns page-level CSS for admin system pages.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Style route composition and page-level states only.
- Do not style shared components through page selectors.
