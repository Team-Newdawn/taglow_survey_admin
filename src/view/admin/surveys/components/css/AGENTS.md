# `src/view/admin/surveys/components/css` Instructions

Owns component CSS for survey list/dashboard widgets.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Component CSS owns card, table, status badge, and action control internals.
- Do not depend on survey page selectors.
