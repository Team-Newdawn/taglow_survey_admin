# `src/view/admin/builder/components/css` Instructions

Owns component CSS for builder panels and editors.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Component CSS owns editor controls, selected states, drag states, validation states, and config layouts.
- Do not reach into builder page shell selectors.
