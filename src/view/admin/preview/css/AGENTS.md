# `src/view/admin/preview/css` Instructions

Owns page-level CSS for `SurveyPreviewPage`.

## Rules

- Use DESIGN.md tokens from `src/components/css/design-tokens.css`.
- Page CSS owns preview route layout, preview/device region, and validation/scenario panel placement.
- Preview component internals belong in `components/css`.
